import { Code2, Database, Layers3, Rocket } from 'lucide-react';
import { getTranslations } from 'next-intl/server';
import type { PortfolioProjects } from '@/features/portfolio/server/get-portfolio-projects';
type Props={projects:PortfolioProjects};const principles=[{id:'interfaces',icon:Code2},{id:'data',icon:Database},{id:'foundations',icon:Layers3},{id:'delivery',icon:Rocket}] as const;
export async function PortfolioAbout({projects}:Props){const t=await getTranslations('PortfolioAbout');const technologies=new Set(projects.flatMap(p=>p.technologies.map(x=>x.slug)));return <section className="border-t border-border py-20 sm:py-24"><div className="grid gap-10 lg:grid-cols-[0.9fr_1.1fr] lg:gap-14"><div className="max-w-xl"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-muted">{t('eyebrow')}</p><h2 className="mt-4 text-3xl font-semibold tracking-[-0.045em] sm:text-4xl lg:text-5xl">{t('titlePrimary')} <span className="text-muted">{t('titleSecondary')}</span></h2><p className="mt-6 text-sm leading-7 text-muted sm:text-base">{t('description')}</p><div className="mt-7 flex flex-wrap gap-2"><span className="rounded-full border border-border bg-surface-muted px-2.5 py-1 font-mono text-[8px] text-muted">{t('publishedProjects',{count:projects.length})}</span><span className="rounded-full border border-border bg-surface-muted px-2.5 py-1 font-mono text-[8px] text-muted">{t('recordedTechnologies',{count:technologies.size})}</span></div></div><div className="grid gap-3 sm:grid-cols-2">{principles.map(item=><article key={item.id} className="rounded-[22px] border border-border bg-background/55 p-5 backdrop-blur-sm"><div className="flex size-9 items-center justify-center rounded-xl border border-border bg-theme-accent-faint"><item.icon className="size-4 text-theme-accent"/></div><h3 className="mt-5 text-sm font-medium">{t(`principles.${item.id}.label`)}</h3><p className="mt-2 text-[11px] leading-5 text-muted">{t(`principles.${item.id}.detail`)}</p></article>)}</div></div></section>}
