RCENTZ M07 preservation fix 1

1. Extract this zip into:
   C:\Users\DeWealth\Desktop\rcentz-systems
   Replace existing files.

2. From the repo root, delete the obsolete carousel from the abandoned first attempt:
   Remove-Item -Force .\features\portfolio\components\PortfolioMediaCarousel.tsx

3. Clear stale Next.js generated route types:
   Remove-Item -Recurse -Force .\.next

4. Run:
   pnpm typecheck
   pnpm lint
   pnpm dev

The img warnings are non-blocking and intentionally left for the preservation pass.
