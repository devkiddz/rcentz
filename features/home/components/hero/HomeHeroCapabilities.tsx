'use client';
import { Boxes, Check, Gauge, LockKeyhole, ServerCog, ShieldCheck, Wrench } from 'lucide-react';
import { motion, useReducedMotion } from 'motion/react';
import { useTranslations } from 'next-intl';
const META=[{id:'performance',icon:Gauge},{id:'security',icon:ShieldCheck},{id:'scalable',icon:Boxes},{id:'maintainable',icon:Wrench}] as const;
export function HomeHeroCapabilities(){const t=useTranslations('HomeHeroCapabilities');const reduceMotion=Boolean(useReducedMotion());return <div className="relative overflow-hidden rounded-[24px] border border-border bg-background/88 shadow-2xl backdrop-blur-xl"><div className="relative z-10 flex items-center justify-between border-b border-border px-4 py-3 sm:px-5"><div className="flex items-center gap-2"><ServerCog className="size-3.5 text-theme-accent"/><span className="font-mono text-[7px] uppercase tracking-[0.15em] text-muted sm:text-[8px]">{t('systemLabel')}</span></div><div className="flex items-center gap-2"><span className="relative flex size-1.5"><span className="absolute inline-flex size-full animate-ping rounded-full bg-theme-accent opacity-25"/><span className="relative inline-flex size-1.5 rounded-full bg-theme-accent"/></span><span className="font-mono text-[6px] uppercase tracking-[0.1em] text-theme-accent sm:text-[7px]">{t('active')}</span></div></div><div className="relative z-10 grid md:grid-cols-2 xl:grid-cols-4">{META.map((item,index)=>{const Icon=item.icon;const base=`items.${item.id}`;return <motion.article key={item.id} initial={reduceMotion?false:{opacity:0,y:10}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:0.25}} transition={{duration:0.6,delay:index*0.06}} className={['group relative min-h-[265px] p-4 hover:bg-surface-raised/55',index%2!==0?'md:border-l md:border-border':'',index>=2?'border-t border-border xl:border-t-0':'',index!==0?'xl:border-l xl:border-border':''].join(' ')}><div className="flex items-start justify-between gap-4"><span className="flex size-10 items-center justify-center rounded-xl border border-border bg-surface-muted"><Icon className="size-4 text-muted group-hover:text-theme-accent"/></span><span className="flex size-11 items-center justify-center rounded-full border border-theme-accent/20 bg-theme-accent-soft"><LockKeyhole className="size-3.5 text-theme-accent"/></span></div><div className="mt-5"><div className="flex items-center justify-between gap-3"><p className="font-mono text-[7px] uppercase tracking-[0.14em] text-theme-accent">{t(`${base}.label`)}</p><span className="flex items-center gap-1.5 font-mono text-[6px] uppercase tracking-[0.08em] text-muted"><span className="size-1 rounded-full bg-theme-accent"/>{t(`${base}.status`)}</span></div><h3 className="mt-2 text-[15px] font-semibold leading-[1.15] tracking-[-0.025em]">{t(`${base}.headline`)}</h3><p className="mt-2.5 text-[9px] leading-[1.7] text-muted sm:text-[10px]">{t(`${base}.description`)}</p></div><div className="mt-4 space-y-2 border-t border-border pt-3">{[0,1,2].map(p=><div key={p} className="flex items-center gap-2"><span className="flex size-4 shrink-0 items-center justify-center rounded-full bg-theme-accent-soft"><Check className="size-2.5 text-theme-accent"/></span><span className="text-[8px] text-muted sm:text-[9px]">{t(`${base}.points.${p}`)}</span></div>)}</div></motion.article>})}</div></div>}
